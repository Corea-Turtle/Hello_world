#include <stdio.h>

main()
{
printf("3");
printf("%d\n", 3);
printf("%d는 %d보다 크다\n, 3, 4);
printf("%d-%d는 %d입니다.\n", 3, 2, 3-2);
printf("%s %c %f \n", "6÷5", '='. 1.2);
}
/* %d 정수(소수점이 붙지 않은 수)를 10진수로 표현한다.
  %f 실수(소수점이 붙어 있는 수)를 표시한다.
  %c 문자(' '로 에워싸인 반각문자 1개)를 표시한다.
  %s 문자열(" "로 에워싸인 문자)을 표시한다.*/
