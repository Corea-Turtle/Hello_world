#include<stdio.h>
void diospnum(int a);

main()
{
      int x = 10;
  dispnum(5);
  dispnum(x);
}

void dispnum(int a)
{
    printf("인수의 값은 %d\n", a);
}

/*
프로토 타입 선언과 호출 부분의
반환값의 형, 인수의 개수, 형, 나열 순서를 같게 해줘야한다.
*/
